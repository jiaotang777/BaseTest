{
  "name": "basetest",
  "version": "0.4.21",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "cf-types": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
